//Generated by FuseIT WSDL2Apex

@isTest
public class OnbeWsManageCardHttpMock implements WebServiceMock {
	public void doInvoke(
			Object stub,
			Object request,
			Map<String, Object> response,
			String endpoint,
			String soapAction,
			String requestName,
			String responseNS,
			String responseName,
			String responseType) {
       
		System.debug(LoggingLevel.INFO, 'OnbeWsManageCard.doInvoke() - ' +
			'\n request: ' + request +
			'\n response: ' + response +
			'\n endpoint: ' + endpoint +
			'\n soapAction: ' + soapAction +
			'\n requestName: ' + requestName +
			'\n responseNS: ' + responseNS +
			'\n responseName: ' + responseName +
			'\n responseType: ' + responseType);

        // the generated code for OnbeWsManageCard is missing some inner classes
	/* 	if(request instanceOf OnbeWsManageCard.) {
			response.put( 'response_x', new OnbeWsManageCard());
		}
		else if(request instanceOf OnbeWsManageCard.) {
			response.put( 'response_x', new OnbeWsManageCard.());
		}
		else if(request instanceOf OnbeWsManageCard.) {
			response.put( 'response_x', new OnbeWsManageCard.());
		}
		else if(request instanceOf OnbeWsManageCard.) {
			response.put( 'response_x', new OnbeWsManageCard.());
		} */
	}
}